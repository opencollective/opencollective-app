machine:
  node:
    version: 5.1.0
deployment:
  staging:
    branch: staging
    heroku:
      appname: opencollective-staging-app
test:
  pre:
    - nohup bash -c "NODE_ENV=test_server npm start &"
